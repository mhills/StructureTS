define(function(require) {
        "use strict"

        /**
         * @class RequestStatusType
         */
        var RequestStatusType = function() {

        };

        RequestStatusType.SUCCESS = "success";
        RequestStatusType.ERROR = "error";

        return RequestStatusType;
    }
);